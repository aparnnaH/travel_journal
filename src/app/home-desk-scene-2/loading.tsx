import AppPageSkeleton from '@/components/loading/PageSkeletons';

// Experimental landing concept uses the same public-page loading shell.
export default function HomeDeskSceneLoading() {
  return <AppPageSkeleton variant="landing" />;
}
